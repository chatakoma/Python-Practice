#A function is a block of code that only runs when you call it.
#The make code organized, reusable, avoid repetition and make big problems easier to build

#Creating a simple function without parameters

def greet():
    print("Hello, Aicha!")
greet()
print("")

#Creating a function with parameters
def greet(name):
    print(f"Hello, {name}!")
greet("Aicha")
greet("Amanda")
print("")

#Creating a function that retuns a value. Note that anything after return does not run
def add(a, b):
    return a + b
    
result = add(2,5)
print(result)

def testing():
    return "done"
    print("This will never run")
print("")
    
#Creating a default Parameter Values
def welcome(name="Aicha"):
    print(f"Welcome {name}!")
welcome() # this will use default value
welcome("Bella")# This will orride default value
print("")

#Creating a function with multiple returns
def stats(a,b):
    return a + b, a*b
    
sum_value, product = stats(4, 6)
print(sum, product)
print("")

#Keyword Arguments. Order doesn't matter when using keyword arguments
def introduce(name, age):
    print(f"{name} is {age} years old. ")
introduce(age=15, name="Aicha")
print("")

#Variable-length Arguments(*args, **kwargs)----->tuple
# *args this handles any numbers. Use if you don't know how many number will be given
def total(*numbers):
    print(sum(numbers))
    
total(1,2,8,9)
total(5,6)

#**kwargs this handles many named values------>dictionary
def profile(**info):
    print(info)

profile(name = "Aicha", age = 15, major="CS")


#Inner Functions: Creating a function inside a function
def outer():
    def inner():
        print("I'm inside!")
    inner()
